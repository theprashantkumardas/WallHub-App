// utils/config.js
// IMPORTANT: If running backend locally and testing on a physical device,
// replace 'localhost' with your computer's local network IP address.
// Find it using `ipconfig` (Windows) or `ifconfig` (macOS/Linux).
// Your phone and computer must be on the same Wi-Fi network.
export const API_BASE_URL = 'https://wallpaper-api-mnb9.onrender.com/api'; // e.g., 'http://192.168.1.5:3001/api'
// If your backend is deployed, use the deployed URL.